# Adds namespace to all resources.
namespace: locust-k8s-operator-system

# Value of this field is prepended to the
# names of all resources, e.g. a deployment named
# "wordpress" becomes "alices-wordpress".
# Note that it should also match with the prefix (text before '-') of the namespace
# field above.
namePrefix: locust-k8s-operator-

# Labels to add to all resources and selectors.
#labels:
#- includeSelectors: true
#  pairs:
#    someName: someValue

resources:
- ../crd
- ../rbac
- ../manager
# [WEBHOOK] To enable admission/conversion webhooks, switch to the
# `config/default-webhook/` overlay (`make deploy-with-webhook`) instead of
# editing this file — the cert-manager wiring lives there. This base overlay
# stays webhook-off so a fresh `make deploy` cannot hit the issue #317
# crash class (no certs available -> CrashLoopBackOff).
# [CERTMANAGER] cert-manager wiring also lives in `config/default-webhook/`.
# [PROMETHEUS] To enable prometheus monitor, uncomment all sections with 'PROMETHEUS'.
#- ../prometheus
# [METRICS] Expose the controller manager metrics service.
- metrics_service.yaml
# [NETWORK POLICY] Protect the /metrics endpoint and Webhook Server with NetworkPolicy.
# Only Pod(s) running a namespace labeled with 'metrics: enabled' will be able to gather the metrics.
# Only CR(s) which requires webhooks and are applied on namespaces labeled with 'webhooks: enabled' will
# be able to communicate with the Webhook Server.
#- ../network-policy

# Uncomment the patches line if you enable Metrics
patches:
# [METRICS] The following patch will enable the metrics endpoint using HTTPS and the port :8443.
# More info: https://book.kubebuilder.io/reference/metrics
- path: manager_metrics_patch.yaml
  target:
    kind: Deployment

# [WEBHOOK]/[CERTMANAGER]/[METRICS-WITH-CERTS] all live in
# config/default-webhook/ (use `make deploy-with-webhook`). That overlay
# brings in ../webhook + ../certmanager, the manager_webhook_patch.yaml,
# and the cert-manager replacements as one unit.
