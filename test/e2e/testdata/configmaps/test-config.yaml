apiVersion: v1
kind: ConfigMap
metadata:
  name: e2e-test-configmap
data:
  locustfile.py: |
    from locust import HttpUser, task

    class TestUser(HttpUser):
        @task
        def hello(self):
            self.client.get("/")
